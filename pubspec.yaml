name: phone_book
description: A simple phone boot to store and retrive your contacts via api.
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=2.18.1 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.3
  chopper: ^5.0.0
  
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.2.1
  chopper_generator: ^5.0.0+1


  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
